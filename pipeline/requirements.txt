# Backend runtime deps for pipeline/api.py (FastAPI + 4 inference models).
# Used by Dockerfile `RUN pip install -r /app/pipeline/requirements.txt`.
#
# NOTE: Dockerfile explicitly installs numpy / torch / scikit-learn / xgboost
# / tensorflow / keras BEFORE this file to lock ABI-critical versions. The
# pins below match those so pip treats them as already-satisfied here.
# tensorflow / keras / scikit-learn are Dockerfile-only and intentionally
# absent from this file (not required for local non-AKI dev workflows).

# --- ABI-critical (must match Dockerfile pins) ---------------------------
numpy==1.26.4
torch==2.2.2
xgboost==3.2.0

# --- Unpinned runtime deps -----------------------------------------------
fastapi
uvicorn
huggingface_hub
pandas
pyarrow
scipy
shap
joblib
